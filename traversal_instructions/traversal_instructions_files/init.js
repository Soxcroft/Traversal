(function($){
	$(function(){
		$(".button-collapse").sideNav();
		/*
		var window_width = $(window).width();

		// Convert rgb to hex value string
		function rgb2hex(rgb) {
			if (/^#[0-9A-F]{6}$/i.test(rgb)) { return rgb; }
			rgb = rgb.match(/^rgb\((\d+),\s*(\d+),\s*(\d+)\)$/);
			if (rgb === null) { return "N/A"; }
			function hex(x) {
				return ("0" + parseInt(x).toString(16)).slice(-2);
			}
			return "#" + hex(rgb[1]) + hex(rgb[2]) + hex(rgb[3]);
		}

		// Detect touch screen and enable scrollbar if necessary
		function is_touch_device() {
			try {
				document.createEvent("TouchEvent");
				return true;
			} catch (e) {
				return false;
			}
		}
		if (is_touch_device()) {
			$('#nav-mobile').css({ overflow: 'auto'});
		}

		// Plugin initialization
		// $('.slider').slider({full_width: true});
		// $('.parallax').parallax();
		// $('.modal-trigger').leanModal();
		// $('.scrollspy').scrollSpy();
		$('#side-collapse').sideNav({'edge': 'left'});
		// $('.datepicker').pickadate({selectYears: 20});
		// $('select').not('.disabled').material_select();

		// Equalizer initialization
		Equalizer('#row-1 .card-content').align();
		Equalizer('#row-2 .card-content').align();
		Equalizer('#row-3 .card-content').align();
		Equalizer('#people-1 .people-content').align();
		Equalizer('#people-2 .people-content').align();
		Equalizer('#people-3 .people-content').align();
		Equalizer('#people-4 .people-content').align();
		*/
	});
})(jQuery);

